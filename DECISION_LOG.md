# Decision log

> One line per meaningful AI call. Keep it lightweight — the point is to show your reasoning, not to write an essay.

| # | What I asked the AI | What it gave back | Accepted / rejected / modified — and why |
|---|---------------------|-------------------|------------------------------------------|
| 1 | Review the backend and frontend monolithic files. List all bugs, focusing on concurrency, state leaks, and memory issues. Don't fix anything yet. | Identified 17 distinct bugs, including mutable default arguments, blocking time.sleep calls, rogue setInterval memory leaks, and direct HTML injection vulnerabilities. | Accepted. I manually verified the critical ones. The uncleaned intervals and shared state issues were spot on. Decided to fix these first before any refactoring. |
| 2 | Apply fixes for the 8 backend bugs in main.py. Swap time.sleep for a thread pool executor, use proper UUIDs, and add Pydantic v2 validators. Keep it all in one file. | A revised main.py with the fixes applied in place. | Accepted. I wanted to verify the core logic in isolation before splitting files. Tested locally and confirmed concurrent requests no longer block the event loop. |
| 3 | Now fix the 9 frontend bugs in App.jsx. Add try/catch blocks, clear the polling interval on unmount, and remove dangerouslySetInnerHTML. | A revised App.jsx addressing network resilience, memory leaks, and security. | Accepted. Stripping out the HTML injection closed a major XSS hole. The app was finally stable, though the codebase was still a mess. |
| 4 | Refactor the backend into a modular structure. Extract env variables to config.py, schemas to models.py, logic to services/run_service.py, and endpoints to api/routes.py. | The new file structure with clean boundaries, leaving main.py as a thin entry point. | Accepted. This was the exact architecture I had in mind. It transformed the messy script into a maintainable backend system. |
| 5 | Do the same for the frontend. Strip fetching into client/runs.js, polling into useRunPoller.js, and split the UI into RunForm.jsx and RunResult.jsx. | Modularized React components and custom hooks separating logic from presentation. | Accepted. Much better separation of concerns. Data fetching and UI are no longer tangled together in one massive component. |
| 6 | Audit the new structure for production readiness. What are we missing before deploying? | Pointed out a lack of observability, hardcoded localhost URLs on the frontend, and missing backend health checks. | Accepted. I had it implement structured logging, dynamic API URL resolution via environment variables, and a /health endpoint for load balancers. |
| 7 | Configure the frontend for Vercel (SPA routing, chunking) and backend for Render (blueprint, Procfile, gunicorn). | Provided vercel.json, updated Vite config, render.yaml, and a Procfile using gunicorn with uvicorn workers. | Accepted. Render initially crashed on its default WSGI command, so wrapping it with gunicorn and uvicorn workers was the exact fix needed. Everything is now live. |
