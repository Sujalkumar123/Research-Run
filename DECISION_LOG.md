# Decision log

> One line per meaningful AI call. Keep it lightweight — the point is to show your reasoning, not to write an essay.

| # | What I asked the AI | What it gave back | Accepted / rejected / modified — and why |
|---|---------------------|-------------------|------------------------------------------|
| 1 | Read through these two massive monolithic files (main.py and App.jsx). Just list all the bugs you see—especially concurrency, memory leaks, or shared state issues. Don't write any fixes yet, I just want a baseline. | It pointed out 17 distinct issues! Spotted some really bad ones like mutable default arguments in Python, blocking time.sleep calls freezing the async loop, and a nasty uncleaned setInterval in React causing memory leaks. It also flagged a big XSS vulnerability with dangerouslySetInnerHTML. | I manually checked the code and it was spot on. I decided we should patch these critical bugs in the monolith first so we can verify the fixes before we start ripping the files apart. |
| 2 | Let's fix the backend first. Give me a new main.py with the 8 backend bugs fixed (swap the sleep for a thread pool, use UUIDs instead of arrays, add Pydantic validators). Keep it all in one file for now. | Generated a revised main.py that patched all 8 issues in place. | Keeping it in one file made it super easy for me to test the core logic in isolation. I ran it locally and confirmed concurrent requests were finally working properly without blocking. |
| 3 | Now let's tackle the frontend. Give me a revised App.jsx fixing the 9 frontend bugs. Add try/catch blocks for the fetches, clean up that polling interval, and get rid of the dangerouslySetInnerHTML. | A rewritten App.jsx with proper error handling, cleanup functions in useEffect, and secure text rendering. | Removing the HTML injection was a huge relief for security. The app finally felt stable, even if the code was still a bit messy. |
| 4 | Alright, the core logic works. Let's refactor the backend into a proper modular structure. Split it into config.py, models.py, services/run_service.py, and api/routes.py. | It provided the exact file structure I asked for, moving the schemas, config, and business logic into their own dedicated files. main.py became just a clean entry point. | This was exactly the architecture I had in mind. It took our messy script and turned it into a real, maintainable backend system. |
| 5 | Let's do the same cleanup on the frontend. Pull the API calls into client/runs.js, move the polling logic into a custom useRunPoller.js hook, and split the UI into RunForm and RunResult components. | It generated the new modular React components and hooks, cleanly separating data fetching from presentation. | The separation of concerns is so much better now. I don't have a massive 200-line component doing everything anymore. |
| 6 | Take a look at our new clean structure. What are we missing before we can actually deploy this to production? | It flagged a few deployment blockers: we had zero logging, the frontend was still hardcoded to hit localhost, and the backend didn't have a /health endpoint. | Good catches. I had it add structured logging, set up environment variables for the API URLs, and add a quick /health check for load balancers. |
| 7 | Let's get this live. Configure the frontend so I can drop it on Vercel, and configure the backend for Render. | It gave me a vercel.json for SPA routing, tweaked Vite's chunking, and wrote a render.yaml blueprint and Procfile for the backend. | Render actually failed at first because of its default WSGI command, so wrapping it in a Procfile with gunicorn and uvicorn workers was the exact fix we needed. Everything is deployed successfully now. |
