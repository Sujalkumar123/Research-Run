# Decision Log

> One line per meaningful AI interaction. Shows reasoning, not an essay.

---

| # | What I asked | What it returned | Accepted / rejected / modified — and why |
|---|-------------|-----------------|------------------------------------------|
| 1 | Read both files. List every production bug grouped by severity — don't fix anything yet. | 8 backend bugs + 9 frontend bugs grouped into: correctness, concurrency, security, UX. | ✅ Accepted. Manually traced each finding against the source. All 17 were real — no false positives. Verified the key ones hands-on: `msg.content` is a list in the Anthropic SDK, mutable default `run_log=[]` is shared state across calls, `setInterval` with no cleanup is a confirmed memory leak. |
| 2 | How does Pydantic v2 `HttpUrl` + `@field_validator` work? Show syntax for validating a non-empty string and a non-empty list. | Confirmed v2 API: `HttpUrl` validates URL format, `@field_validator` with `@classmethod` replaces deprecated `@validator`. Working examples for both. | ✅ Accepted after verification. Ran `python -c` test — `ValidationError` fires correctly on empty strings and empty lists before writing any model code. |
| 3 | Generate fixed `main.py` with all 8 backend fixes: UUID run IDs, HTTPException 404, env-var CORS, Pydantic validators, `run_in_executor` for blocking code, try/except in `process_run`, `content[0].text` extraction, remove mutable default. | Complete file with all 8 fixes applied in one pass. | ✅ Accepted after review. Compiled with `py_compile` — no errors. Cross-checked every fix against the bug list. Intentionally kept as a single file here — deferred structural cleanup to a separate pass so bug fixes and refactoring stay independently verifiable. |
| 4 | Generate fixed `App.jsx` with all 9 frontend fixes: loading/error state, try/catch on fetches, `clearInterval` cleanup, URL trimming + blank line filtering, client-side validation, key props, remove `dangerouslySetInnerHTML`, handle `error` status, format success rate and timestamp. | Complete component with all 9 fixes. | ✅ Accepted after targeted checks. Confirmed `useEffect` deps include `run?.status` so polling stops on terminal status. Confirmed `dangerouslySetInnerHTML` is fully removed — text search, not just eyeballed. |
| 5 | Write the sign-off note. | Structured note with bug categories, fix rationale, and 5 "would block" items for real traffic. | ✏️ Modified. Trimmed "would block" to only items genuinely outside scope — persistent storage, rate limiting, call timeouts. The AI included things already fixed; a senior distinction is knowing what's a real blocker vs. a future enhancement. |
| 6 | Audit the first-pass solution for remaining production gaps: observability, deployment readiness, code organisation, Python version compatibility. | 6 backend gaps: no logging, deprecated `asyncio.get_event_loop()` (raises `RuntimeError` on 3.12), no `/health`, single-file monolith, unnecessary `allow_credentials`, CORS env-var without `.strip()`. 6 frontend gaps: no `console.error`, hardcoded localhost URL, first poll failure kills polling, 200-line monolith, `key={i}` index key, `// Fix N:` patch comments. | ✅ Accepted. Verified each against the live server. `asyncio.get_event_loop()` confirmed on Python 3.12. This pass is what separates "it works" from "I'd put my name on this." |
| 7 | Refactor into modular structure and fix all 12 remaining gaps. Backend: `config.py`, `models.py`, `api/routes.py`, `services/run_service.py`, thin `main.py`. Frontend: `client/runs.js`, `hooks/useRunPoller.js`, `components/RunForm.jsx`, `components/RunResult.jsx`, thin `App.jsx`. I defined the exact file boundaries upfront — module structure is an architectural decision, not a generation task. | Complete modular codebase with all 12 gaps fixed: structured logging, `get_running_loop()`, `/health` endpoint, `import.meta.env.VITE_API_URL`, 3-strike polling retry, `key={s}` on sources, WHY-based comments throughout. | ✅ Accepted after end-to-end verification. Re-ran all 11 edge-case curl tests against the reloaded server — all pass. Structured log lines appear on `POST /runs` and completion. `/health` returns 200. 3 concurrent runs complete independently with unique UUIDs. |
| 8 | Make frontend deployable on Vercel. | Added `vercel.json` for routing and rewritten paths, configured custom `manualChunks` in `vite.config.js` to support Vite 8's Rolldown bundler, and added meta tags for SEO/social sharing. | ✅ Accepted. Built the project locally with `npm run dev` and `npm run build` to confirm bundle builds without Rolldown errors. |
| 9 | Make backend deployable on Render. | Created `render.yaml` Blueprint spec at the root directory to define the web service with appropriate build and start commands (`uvicorn`). | ✅ Accepted. Simplifies deployment process via Render infrastructure-as-code blueprints. |
